[core]
    editor = code
	autocrlf = input

[merge]
    tool = nvim

[alias]
    st = "status -s -b"
    co = "checkout"
    cb = "checkout -b"
    cm = "commit -m"
    cam = "commit -a -m"
    sc = "switch -c"
    cp = "cherry-pick"
    f = "fetch"

[user]
	email = liamlemblake@gmail.com
	name = LiamBlake
