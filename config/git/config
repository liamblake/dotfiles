[core]
	editor = nvim
	autocrlf = input

[mergetool "nvim"]
	cmd = nvim -f -c \"Git mergetool\" \"$MERGED\"

[merge]
	tool = nvim

[mergetool]
	prompt = false

[alias]
	st = "status -s -b"
	co = "checkout"
	cb = "checkout -b"
	cm = "commit -m"
	cam = "commit -a -m"
	sc = "switch -c"
	cp = "cherry-pick"
	f = "fetch"
	pl = "log --graph --oneline --decorate"

[user]
	email = liamlemblake@gmail.com
	name = LiamBlake
