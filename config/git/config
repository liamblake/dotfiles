[core]
    editor = code
	autocrlf = input

[merge]
    tool = code

[alias]
    st = "status -s -b"
    co = "checkout"
    cb = "checkout -b"
    cm = "commit -m"
    cam = "commit -a -m"
    sc = "switch -c"
    cp = "cherry-pick"

[user]
	email = liamlemblake@gmail.com
	name = LiamBlake
